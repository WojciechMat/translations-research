# Brutal translator configuration
defaults:
  - data: default
  - _self_

translator: brutal

dictionary:
  path: "tmp/dictionaries/en_pl_dictionary.json"
  keep_unknown: true
  lowercase: true
  build: false
  source: "gemini"

slice_spec: ":10"  # Default to first 10 examples
